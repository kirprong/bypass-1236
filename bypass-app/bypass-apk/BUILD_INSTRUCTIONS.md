# 🔥 BYPASS-1236 - Инструкции по сборке APK

## ✅ Что уже сделано

Flutter проект полностью готов к сборке:

- ✅ Создана структура проекта Flutter
- ✅ Настроены все зависимости (Provider, just_audio, shared_preferences, и т.д.)
- ✅ Реализована логика таймера с фазами 1-2-3-6
- ✅ Создан главный экран с таймером
- ✅ Реализована система статистики и рангов
- ✅ Создан экран paywall для монетизации
- ✅ Добавлены звуковые файлы
- ✅ Настроены провайдеры для state management
- ✅ Реализована вибрация и звуковые эффекты

## 📱 Способы сборки APK

### Способ 1: Установить Android Studio (Локальная сборка)

**Шаги:**

1. **Скачайте и установите Android Studio**
   - Ссылка: https://developer.android.com/studio
   - Установите все компоненты по умолчанию

2. **Откройте Android Studio SDK Manager**
   - Откройте Android Studio
   - Перейдите в `Tools > SDK Manager`
   - Установите:
     - ✅ Android SDK Platform (API 21 или выше)
     - ✅ Android SDK Build-Tools
     - ✅ Android SDK Command-line Tools
     - ✅ Google Play Services

3. **Настройте переменную окружения ANDROID_HOME**
   ```
   Путь обычно: C:\Users\<ваше_имя>\AppData\Local\Android\Sdk
   ```
   
   **Настройка в Windows:**
   - Откройте "Система" > "Дополнительные параметры системы"
   - Нажмите "Переменные среды"
   - Создайте новую системную переменную:
     - Имя: `ANDROID_HOME`
     - Значение: `C:\Users\<ваше_имя>\AppData\Local\Android\Sdk`
   - Добавьте в PATH:
     - `%ANDROID_HOME%\platform-tools`
     - `%ANDROID_HOME%\tools`

4. **Проверьте установку**
   ```bash
   flutter doctor
   ```
   Должно показать, что Android toolchain установлен.

5. **Соберите APK**
   ```bash
   cd C:\Users\1\first\BYPASS-1236\bypass-app\bypass-apk
   flutter build apk --release
   ```

6. **Найдите готовый APK**
   ```
   C:\Users\1\first\BYPASS-1236\bypass-app\bypass-apk\build\app\outputs\flutter-apk\app-release.apk
   ```

---

### Способ 2: Онлайн сборка через Codemagic (БЕЗ Android Studio)

**Преимущества:**
- ✅ Не нужно устанавливать Android Studio
- ✅ Сборка в облаке
- ✅ Бесплатно для небольших проектов

**Шаги:**

1. **Создайте аккаунт на Codemagic**
   - Перейдите на https://codemagic.io
   - Зарегистрируйтесь (можно через GitHub)

2. **Загрузите проект**
   - Создайте Git репозиторий или загрузите ZIP
   - Подключите репозиторий к Codemagic

3. **Настройте сборку**
   - Выберите "Flutter App"
   - Platform: Android
   - Build type: Release

4. **Запустите сборку**
   - Нажмите "Start new build"
   - Дождитесь завершения (5-10 минут)

5. **Скачайте APK**
   - После сборки появится ссылка на скачивание APK

---

### Способ 3: AppGyver / FlutLab (Онлайн IDE)

**FlutLab** - онлайн редактор Flutter с возможностью сборки APK.

1. Зайдите на https://flutlab.io
2. Создайте новый проект
3. Загрузите файлы проекта
4. Нажмите "Build APK"
5. Скачайте готовый APK

---

### Способ 4: GitHub Actions (Автоматическая сборка)

Если у вас есть GitHub репозиторий:

1. **Создайте файл** `.github/workflows/build-apk.yml`

```yaml
name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'
        channel: 'stable'
    
    - name: Get dependencies
      run: flutter pub get
    
    - name: Build APK
      run: flutter build apk --release
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: app-release
        path: build/app/outputs/flutter-apk/app-release.apk
```

2. **Закоммитьте и запушьте**
   ```bash
   git add .
   git commit -m "Add build workflow"
   git push
   ```

3. **Скачайте APK из Actions**
   - Перейдите на вкладку "Actions" в GitHub
   - Найдите последний workflow
   - Скачайте артефакт "app-release"

---

## 🚀 Быстрый старт (без сборки)

Если вы хотите просто протестировать приложение:

```bash
# Запустите на эмуляторе или подключенном устройстве
cd C:\Users\1\first\BYPASS-1236\bypass-app\bypass-apk
flutter run
```

---

## 🐛 Troubleshooting

### Ошибка: "No Android SDK found"
**Решение:** Установите Android Studio и настройте ANDROID_HOME (см. Способ 1)

### Ошибка: "Gradle build failed"
**Решение:** 
```bash
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
flutter build apk --release
```

### Ошибка: "SDK version mismatch"
**Решение:** Обновите Flutter SDK
```bash
flutter upgrade
flutter doctor
```

### Проблемы со звуком
**Проверьте:** Все звуковые файлы должны быть в `assets/sounds/`:
- bolt.mp3
- ignite.mp3
- rest.mp3
- scan.mp3
- siren.mp3
- start.mp3

---

## 📦 Размер APK

Примерный размер готового APK:
- **Release APK**: ~40-50 MB
- **Split APK (arm64-v8a)**: ~25-30 MB

Для уменьшения размера используйте:
```bash
flutter build apk --split-per-abi
```

---

## 🎨 Кастомизация

### Изменить цвета фаз
Файл: `lib/utils/constants.dart`

### Изменить длительность фаз
Файл: `lib/utils/constants.dart`
```dart
static const int phase1Duration = 60;  // 1 минута
static const int phase2Duration = 120; // 2 минуты
static const int phase3Duration = 180; // 3 минуты
static const int phase4Duration = 360; // 6 минут
```

### Изменить название приложения
Файл: `android/app/src/main/AndroidManifest.xml`
```xml
<application
    android:label="BYPASS-1236"
    ...>
```

### Изменить иконку приложения
Замените файлы в:
- `android/app/src/main/res/mipmap-*/ic_launcher.png`

Или используйте пакет `flutter_launcher_icons`:
```bash
flutter pub add flutter_launcher_icons
flutter pub run flutter_launcher_icons
```

---

## ✅ Checklist перед сборкой

- [ ] Все зависимости установлены (`flutter pub get`)
- [ ] Нет ошибок анализа (`flutter analyze`)
- [ ] Звуковые файлы на месте в `assets/sounds/`
- [ ] Android SDK настроен (для локальной сборки)
- [ ] Версия и имя приложения корректны

---

## 🎯 Следующие шаги после сборки

1. **Протестируйте APK** на реальном устройстве
2. **Настройте signing** для публикации в Google Play
3. **Добавьте иконку** и splash screen
4. **Настройте analytics** (Firebase, Amplitude)
5. **Интегрируйте платежи** (In-App Purchases)

---

**BYPASS-1236 готов к сборке! 🚀**

Удачи с созданием APK! Если возникнут вопросы - обращайся.
