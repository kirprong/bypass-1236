# ИЗМЕНЕНИЯ - Звуки и Автоматические переходы

## ✅ Что было реализовано (согласно 1236.md)

### 1. **Звуковая система (Audio Anchors)**

Реализована полная звуковая система согласно спецификации из markdown (строки 80-88, 322-355):

#### Файл: `lib/utils/constants.dart`
- ✅ `soundStartThinking` = `scan.mp3` - Переход к фазе 1 (начало мышления)
- ✅ `soundPrepPhase` = `bolt.mp3` - Переход 1→2 (щелчок затвора)
- ✅ `soundStrikePhase` = `siren.mp3` - Переход 2→3 (сирена)
- ✅ `soundRecovery` = `rest.mp3` - Переход к отдыху
- ✅ `soundInertiaActive` = `ignite.mp3` - Активация инерции (форсаж)
- ✅ `soundStart` = `start.mp3` - Нажатие Start/Pause
- ✅ `soundWarning` = `beep.mp3` - Предупреждение за 6 секунд
- ✅ `soundFinish` = `finish.mp3` - Окончание фазы
- ✅ `soundDeadManSwitch` = `beep.mp3` - Dead Man's Switch (повторяющийся)

#### Файл: `lib/services/audio_service.dart`
Добавлены методы:
- ✅ `playPhaseSound(int phaseIndex)` - Звук при переходе между фазами
- ✅ `playInertiaSound()` - Звук активации инерции
- ✅ `playStartSound()` - Звук при нажатии Start/Pause
- ✅ `playWarningSound()` - Предупреждение за 6 секунд
- ✅ `playFinishSound()` - Звук окончания фазы
- ✅ `playDeadManSwitchSound()` - Повторяющийся beep

---

### 2. **Автоматические переходы 1→2→3** (строки 451-460)

Реализована логика "Цепной реакции":

#### Файл: `lib/providers/timer_provider.dart`

**Метод `_onPhaseComplete()`:**
```dart
// АВТОМАТИЧЕСКИЕ ПЕРЕХОДЫ: 1 → 2 → 3
if (_currentPhaseIndex < 2) {
  _currentPhaseIndex++;
  _remainingSeconds = AppConstants.getPhaseDuration(_currentPhaseIndex);
  
  final now = DateTime.now().millisecondsSinceEpoch;
  _targetEndTimeMillis = now + (_remainingSeconds * 1000);
  _hasPlayedWarning = false;
  
  _audioService.playPhaseSound(_currentPhaseIndex);
  
  debugPrint('TIMER: Автоматический переход на фазу $_currentPhaseIndex');
  return;
}
```

✅ **Фаза 1 (1 мин) → автоматически → Фаза 2 (2 мин)**
- Звук: `bolt.mp3`
- Без остановки таймера

✅ **Фаза 2 (2 мин) → автоматически → Фаза 3 (3 мин)**
- Звук: `siren.mp3`
- Без остановки таймера

---

### 3. **Ручной выбор после Фазы 3** (строки 461-468)

После завершения Фазы 3 (STRIKE) таймер останавливается и показывает выбор:

```dart
// После фазы 3 (STRIKE) - РУЧНОЙ ВЫБОР
if (_currentPhaseIndex == 2) {
  _isRunning = false;
  _isWaitingForChoice = true;
  _timer?.cancel();
  WakelockPlus.disable();
  
  // Запускаем Dead Man's Switch
  _startDeadManSwitch();
  
  debugPrint('TIMER: Фаза 3 завершена. Ожидание выбора: Инерция или Отдых');
}
```

#### Файл: `lib/screens/main_screen.dart`

В `_buildControls()` добавлена логика показа кнопок выбора:

```dart
if (timer.isWaitingForChoice) {
  return Column(
    children: [
      // Кнопка ИНЕРЦИЯ (только для премиум)
      if (timer.isPremium) ...[
        _buildMainButton(
          text: '⚡ ИНЕРЦИЯ',
          color: AppConstants.inertiaColor,
          onTap: () => timer.activateInertia(),
          glow: true,
        ),
        const SizedBox(height: 15),
      ],
      
      // Кнопка ОТДЫХ (доступна всем)
      _buildMainButton(
        text: '🧠 ОТДЫХ',
        color: AppConstants.phase4Color,
        onTap: () => timer.startRecovery(),
        textColor: Colors.black,
      ),
    ],
  );
}
```

✅ **Премиум пользователи видят:**
- ⚡ ИНЕРЦИЯ (золотая, с эффектом свечения)
- 🧠 ОТДЫХ (зеленая)

✅ **Бесплатные пользователи видят:**
- 🧠 ОТДЫХ (зеленая)
- Подсказка: "Режим ИНЕРЦИЯ доступен только в премиум"

---

### 4. **Dead Man's Switch** (строки 477-478)

Если пользователь не сделал выбор в течение 30 секунд после фазы 3:

```dart
void _startDeadManSwitch() {
  _deadManSwitchTimer?.cancel();
  
  _deadManSwitchTimer = Timer.periodic(
    const Duration(seconds: AppConstants.deadManSwitchSeconds),
    (timer) {
      // Повторяющийся противный звук
      _audioService.playDeadManSwitchSound();
      debugPrint('TIMER: Dead Man\'s Switch активирован! Выбери действие!');
    },
  );
}
```

✅ Каждые 30 секунд играет звук `beep.mp3`
✅ Останавливается при выборе Инерции или Отдыха

---

### 5. **Звуковое предупреждение за 6 секунд** (строка 481)

```dart
// Предупреждение за 6 секунд до конца
if (remaining == AppConstants.warningBeforeEndSeconds && !_hasPlayedWarning) {
  _audioService.playWarningSound();
  _hasPlayedWarning = true;
  debugPrint('TIMER: Предупреждение за 6 секунд до конца фазы $_currentPhaseIndex');
}
```

✅ За 6 секунд до конца КАЖДОЙ фазы играет `beep.mp3` (короткий)
✅ Играет только один раз (флаг `_hasPlayedWarning`)

---

### 6. **Звук окончания фазы** (строка 481)

```dart
void _onPhaseComplete() {
  _audioService.playFinishSound(); // Звук окончания фазы
  _vibratePhase();
  // ...
}
```

✅ При завершении КАЖДОЙ фазы играет `finish.mp3`
✅ + вибрация по паттерну `[0, 500, 200, 500]`

---

### 7. **Звук при старте первой фазы** (scan.mp3)

```dart
void _start() {
  _isRunning = true;

  // При старте первой фазы играем scan.mp3
  if (_currentPhaseIndex == 0 && _remainingSeconds == AppConstants.phase1Duration) {
    _audioService.playPhaseSound(0);
  }
  // ...
}
```

✅ При первом нажатии СТАРТ играет `scan.mp3`
✅ Это звук "выбора цели" / "лазерного наведения"

---

## 📊 Схема работы звуков

| Событие | Звук | Когда |
|---------|------|-------|
| Нажатие СТАРТ/ПАУЗА | `start.mp3` | Всегда при клике |
| Начало Фазы 1 | `scan.mp3` | При первом старте |
| Переход 1→2 | `bolt.mp3` | Автоматически |
| Переход 2→3 | `siren.mp3` | Автоматически |
| Активация Инерции | `ignite.mp3` | При нажатии ⚡ ИНЕРЦИЯ |
| Переход к Отдыху | `rest.mp3` | При нажатии 🧠 ОТДЫХ |
| За 6 сек до конца | `beep.mp3` | Перед окончанием каждой фазы |
| Окончание фазы | `finish.mp3` | При завершении каждой фазы |
| Dead Man's Switch | `beep.mp3` (повтор) | Каждые 30 сек если нет выбора |

---

## 🎯 Логика переходов (итого)

### Связка 1→2→3 (Автоматическая цепная реакция)
```
СТАРТ → [Фаза 1: 1 мин] → bolt.mp3 → [Фаза 2: 2 мин] → siren.mp3 → [Фаза 3: 3 мин] → finish.mp3 → ВЫБОР
```

✅ Первые 6 минут (1+2+3) — один неделимый рывок
✅ Пользователь не может остановиться между фазами 1-2-3
✅ Создаёт давление: "Время идёт, я должен херачить!"

### Момент Истины (после Фазы 3)
```
[Конец Фазы 3] → finish.mp3 + вибрация → Таймер СТОП → Экран выбора:
  - ⚡ ИНЕРЦИЯ (премиум) → ignite.mp3 → Счётчик вверх
  - 🧠 ОТДЫХ (все) → rest.mp3 → 6 минут отдыха
  
Если нет выбора 30 сек → beep.mp3 (повтор каждые 30 сек)
```

### Переход 6→1 (Ручной)
```
[Конец Отдыха] → finish.mp3 + вибрация → Таймер СТОП → Кнопка СТАРТ доступна
```

✅ Новый цикл начинается ТОЛЬКО вручную
✅ Пользователь должен сам нажать СТАРТ

---

## 🚀 Как протестировать

### Тест 1: Автоматические переходы
1. Нажать СТАРТ
2. Услышать `scan.mp3` (начало фазы 1)
3. Подождать 1 минуту → услышать `finish.mp3` + `bolt.mp3` → автоматический переход на фазу 2
4. Подождать 2 минуты → услышать `finish.mp3` + `siren.mp3` → автоматический переход на фазу 3
5. Подождать 3 минуты → услышать `finish.mp3` → экран выбора

✅ **Ожидаемый результат:** Таймер не останавливается между фазами 1-2-3

### Тест 2: Предупреждение за 6 секунд
1. Запустить таймер
2. За 6 секунд до конца фазы услышать `beep.mp3`

✅ **Ожидаемый результат:** Короткий beep за 6 сек до конца

### Тест 3: Выбор после фазы 3 (Премиум)
1. Включить премиум в настройках
2. Дойти до конца фазы 3
3. Увидеть две кнопки: ⚡ ИНЕРЦИЯ и 🧠 ОТДЫХ
4. Нажать ⚡ ИНЕРЦИЯ → услышать `ignite.mp3`

✅ **Ожидаемый результат:** Счётчик инерции начинает расти (+00:01, +00:02...)

### Тест 4: Выбор после фазы 3 (Бесплатный)
1. Отключить премиум в настройках
2. Дойти до конца фазы 3
3. Увидеть только кнопку 🧠 ОТДЫХ + подсказку
4. Нажать 🧠 ОТДЫХ → услышать `rest.mp3`

✅ **Ожидаемый результат:** Начинается фаза 4 (6 минут отдыха)

### Тест 5: Dead Man's Switch
1. Дойти до конца фазы 3
2. НЕ нажимать ничего
3. Через 30 секунд услышать повторяющийся `beep.mp3`

✅ **Ожидаемый результат:** Противный beep каждые 30 секунд

---

## 📝 Итоговая сводка изменений

| Файл | Изменения |
|------|-----------|
| `lib/utils/constants.dart` | ✅ Добавлены константы для новых звуков и таймингов |
| `lib/services/audio_service.dart` | ✅ Добавлены методы для всех звуков |
| `lib/providers/timer_provider.dart` | ✅ Реализованы автоматические переходы, Dead Man's Switch, предупреждения |
| `lib/screens/main_screen.dart` | ✅ Добавлен экран выбора после фазы 3 |

---

## ✅ Все требования выполнены

- ✅ Звук `scan.mp3` при старте первой фазы
- ✅ Автоматические переходы 1→2→3
- ✅ Звуки при переходах (bolt.mp3, siren.mp3)
- ✅ Звук предупреждения за 6 секунд (beep.mp3)
- ✅ Звук окончания фазы (finish.mp3)
- ✅ Ручной выбор после фазы 3 (Инерция или Отдых)
- ✅ Dead Man's Switch (30 сек → beep.mp3)
- ✅ Кнопка ОТДЫХ для всех пользователей
- ✅ Кнопка ИНЕРЦИЯ только для премиум

**Приложение полностью соответствует спецификации из 1236.md!** 🎉
